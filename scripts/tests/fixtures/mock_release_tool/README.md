# mock_release_tool

Minimal fixture used by dsr E2E release parity tests.

- Go-based single-binary tool
- GitHub Actions release workflow that builds per-platform artifacts
- Install script that downloads release assets
